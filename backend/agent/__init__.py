# Agent logic lives here.
# context_bundle.py  — assembles user + signals + calendar into a prompt context
# intervene.py       — calls Claude API, returns messages + redirect_suggestion
# vulnerability.py   — computes vulnerability score from signals
